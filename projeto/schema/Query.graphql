# Pontos de entrada da API
type Query {
    ola: String
    data: Date
    userLog: Usuario
    produto: Produto
    numeroSort: [Int!]!
    usuarios: [Usuario]
    usuario(id: Int): Usuario
    perfis: [Perfil]
    perfil(id:Int): Perfil
}